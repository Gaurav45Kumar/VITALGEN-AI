# VitalGen - Privacy-Preserving Synthetic Medical Data Platform

Complete implementation matching your architecture:
- Tabular EHR synthetic data (CTGAN + Differential Privacy)
- Medical Image synthesis using Diffusion Models
- Agent-based workflow (Ingestion → Privacy → Training → Generation → Validation)
- TSTR (Train on Synthetic, Test on Real) evaluation
- Docker support for hospital secure zone

## How to Run
```bash
pip install -r requirements.txt
python main.py